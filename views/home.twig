{% extends "template.twig" %}

{% block title %}
    <title>Accueil</title>
{% endblock %}


{% block body %}

<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Email</th>
      <th scope="col">Mot de passe(PAS bien de faire ça) !</th>
    </tr>
  </thead>
  <tbody>
    {% for user in users %}
        <tr>
            <th>
                {{user.id}}
            </th>
            <td>
                {{user.email}}
            </td>
            <td>
                {{user.password}}
            </td>
        </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}